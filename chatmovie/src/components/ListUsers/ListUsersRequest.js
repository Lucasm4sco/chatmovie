import { ContainerUsers } from "./styles"
import { Text } from "react-native"

const ListUsersRequest = () => {
    return (
        <ContainerUsers>
            <Text style={{color: 'white'}}>List users request</Text>
        </ContainerUsers>
    )
}

export default ListUsersRequest