import { View, Text } from "react-native";

const WithoutConnectionScreen = () => (
    <View>
        <Text style={{color: '#AAA'}}>Sem conexão com a internet </Text>
    </View>
);

export default WithoutConnectionScreen;