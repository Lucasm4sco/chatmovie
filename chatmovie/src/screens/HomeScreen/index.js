import { Text, View} from "react-native"

const HomeScreen = () => {
    return <View style={{backgroundColor: '#121212', flex: 1}}>
        <Text >Home</Text>
    </View>
}

export default HomeScreen;