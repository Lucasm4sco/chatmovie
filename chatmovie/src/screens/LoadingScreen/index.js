import { View, Text } from 'react-native';

const LoadingScreen = () => {
    return (
        <View>
            <Text style={{color: '#AAA'}}>Carregando...</Text>
        </View>
    )
}

export default LoadingScreen